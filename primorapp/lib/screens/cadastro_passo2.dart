import 'package:flutter/material.dart';

class CadastroPasso2 extends StatelessWidget {
  const CadastroPasso2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Passo 2")),
      body: const Center(child: Text("Em breve: Especialidades")),
    );
  }
}